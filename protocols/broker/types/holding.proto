syntax = "proto2";
package broker;

import 'investorId.proto';
import 'lot.proto';
import 'symbol.proto';

message Holding {
    required InvestorId investorId = 1;
    required Symbol symbol = 2;
    required double amount = 3;

    repeated Lot lots = 300;
}